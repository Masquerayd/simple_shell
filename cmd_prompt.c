#include "shell.h"

/**
 * _sh_prompt - A function that displays a prompt for the user to
 * type in commands.
 *
 * Return: void.
 */

void _sh_prompt(void)
{
	while (0 < 1)
	{
		_sh_print("customSheel$ ");
	}
}
